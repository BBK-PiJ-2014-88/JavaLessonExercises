public class DoublyLinkedListImplementation<T> implements DoublyLinkedList<T>{
	T firstElement = null;

	public DoublyLinkedListImplementation(){
	T value;
	T nextElement;
	T previousElement;
	}

	public void insert(T element){
		if (firstElement == null){
			firstElement = element;
		}
		else{
			if (
		}
	}
}